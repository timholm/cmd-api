apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmd-api.fullname" . }}
  labels:
    {{- include "cmd-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "cmd-api.selectorLabels" . | nindent 4 }}
